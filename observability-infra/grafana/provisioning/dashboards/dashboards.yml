apiVersion: 1

providers:
  - name: "VulnerableApp dashboards"
    orgId: 1
    folder: "VulnerableApp"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: false
    options:
      path: /etc/grafana/provisioning/dashboards/json
